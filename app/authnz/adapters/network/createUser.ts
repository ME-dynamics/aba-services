
// ! temp id generator ==> remove later
import { v4 } from "uuid"

/**
 * sync request to user service, using http or gRPC
 * create a user on user verification
 * @returns {string} user id
 */
export function buildCreateUser() {
  return async function createUser(otpId: string) {
      return true
  };
}
